<svg width="400" height="400" viewBox="0 0 400 400" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <radialGradient id="pinkGradient" cx="50%" cy="40%" r="60%">
      <stop offset="0%" stop-color="#ffd6e7"></stop>
      <stop offset="100%" stop-color="#f7a8c4"></stop>
    </radialGradient>

    <style>
      @import url('https://fonts.googleapis.com/css2?family=Pacifico&amp;display=swap');

      .about-text {
        font-family: "Pacifico", cursive;
        font-size: 34px;
        fill: #ffffff;
        font-weight: 500;
        <!-- letter-spacing: 0.5px; -->
        <!-- font-style: italic; -->
      }
    </style>
  </defs>

  <!-- Circle -->
  <circle cx="200" cy="200" r="170" fill="url(#pinkGradient)"></circle>

  <!-- Centered Text -->
  <text x="200" y="210" text-anchor="middle" class="about-text">
    <!-- About Me -->
  </text>
</svg>